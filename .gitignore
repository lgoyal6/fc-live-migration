# Build artifacts (Go binaries, Firecracker binary, kernel, rootfs image).
build/

# Benchmark output (CSV, plots) — regenerated by `make bench`.
bench-results/

# Local scratch.
*.log
.DS_Store

# Vendored Firecracker clone for patch development (built in the VM).
third_party/

# graphify knowledge graph (local index, not source)
graphify-out/
